version: 2

build:
  os: "ubuntu-22.04"
  tools:
    python: "mambaforge-latest"

conda:
  environment: doc/environment_rtd.yml

python:
  install:
    - method: pip
      path: .
      
sphinx:
  configuration: doc/source/conf.py
